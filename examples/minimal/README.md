# Minimal Example

The simplest PO Agent setup — GitHub PR comments only.

## Setup

1. Copy `po-agent.yml` to `.github/workflows/po-agent.yml` in your repo
2. Add `ANTHROPIC_API_KEY` to your repo secrets
3. Comment `@po-agent fix this bug` on any PR
